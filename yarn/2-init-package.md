# yarn init

可以创建一个 `package.json` 文件.

```js
$ yarn init
```

#### `yarn init --yes/-y`

使用默认值创建 `package.json`.
<br>
<br>

#### `yarn init --private/-p`

> 自动添加 private: true 到 package.json

同时使用 -y 和 -p:

```js
yarn init -yp
```
